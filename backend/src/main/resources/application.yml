server:
  port: 8080

spring:
  application:
    name: ledgerone-backend
  datasource:
    url: jdbc:postgresql://ep-autumn-waterfall-atvjxk1j-pooler.c-9.us-east-1.aws.neon.tech/LedgerOne?user=neondb_owner&password=REDACTED_NEON_PASSWORD&sslmode=require&channelBinding=require
    username: neondb_owner
    password: REDACTED_NEON_PASSWORD
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  data:
    redis:
      url: "rediss://default:REDACTED_UPSTASH_TOKEN@literate-termite-139779.upstash.io:6379"
      repositories:
        enabled: false
  cache:
    type: redis
  mail:
    host: smtp.gmail.com
    port: 587
    username: chintukasundra060106@gmail.com
    password: REDACTED_GMAIL_APP_PASSWORD_2
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET:ledgerone-auth-service-dev-secret-key-change-in-prod}
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

logging:
  level:
    com.ledgerone: DEBUG

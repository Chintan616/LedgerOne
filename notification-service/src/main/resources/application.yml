server:
  port: 8084

spring:
  application:
    name: notification-service
  cloud:
    compatibility-verifier:
      enabled: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: chintukasundra060106@gmail.com
    password: REDACTED_GMAIL_APP_PASSWORD_1
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.ledgerone: DEBUG
